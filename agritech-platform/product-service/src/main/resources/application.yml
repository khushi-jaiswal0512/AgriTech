spring:
  application:
    name: product-service
  config:
    import: "optional:configserver:http://${CONFIG_SERVER_USERNAME:config}:${CONFIG_SERVER_PASSWORD:config-secret}@${CONFIG_SERVER_HOST:localhost}:${CONFIG_SERVER_PORT:8888}"
